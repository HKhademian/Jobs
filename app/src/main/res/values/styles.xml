<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
    <item name="android:windowAnimationStyle">@style/WindowAnimationStyle</item>
  </style>

  <style name="AppTheme.NoActionBar">
    <item name="windowActionBar">false</item>
    <item name="windowNoTitle">true</item>
  </style>


  <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

  <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

  <style name="WindowAnimationStyle">
    <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
    <item name="android:windowExitAnimation">@android:anim/fade_out</item>
  </style>

  <style name="CardView.Light.Hidden" parent="CardView.Light">
    <item name="android:background">@null</item>
    <item name="cardBackgroundColor">@null</item>
    <item name="cardCornerRadius">0dp</item>
    <item name="cardElevation">0dp</item>
    <item name="cardPreventCornerOverlap">false</item>
  </style>

  <declare-styleable name="FieldBox">
    <attr format="string" name="android:text"/>
    <attr format="string" name="action"/>
    <attr format="string" name="action1"/>
    <attr format="string" name="action2"/>
    <attr format="string" name="action3"/>
  </declare-styleable>

  <declare-styleable name="JobView">
    <attr format="string" name="jobId"/>
    <attr format="boolean" name="small"/>
  </declare-styleable>

  <declare-styleable name="SkillView">
    <attr format="string" name="skillId"/>
  </declare-styleable>
</resources>
